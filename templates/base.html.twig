<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="assets/bootstrap.css">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Notable&display=swap" rel="stylesheet">
        <style>

        

        /* Style de base pour la barre de navigation */
        .navbar {
        background-color:  #FFD1DV; /* Couleur de fond de la barre de navigation */
        color: black; /* Couleur du texte */
        padding: 15px 0; /* Espacement intérieur de la barre de navigation */
        display: flex; /* Utiliser Flexbox pour centrer les éléments */
        justify-content: space-between; /* Espacement équitable entre les éléments */
         align-items: center; /* Centrer verticalement */
        }

        /* Style pour le texte du logo */
        .navbar-brand {
        font-family: 'Notable', sans-serif;
        font-size: 24px;
        cfont-weight: bold;
        color: black; /* Couleur du texte du logo */
         transition: color 1s; /* Ajoute une transition de couleur fluide */
        }

        /* Style pour les liens de la barre de navigation */
        .navbar-nav .nav-link {
        font-size: 16px;
        color: black; /* Couleur du texte des liens */
        margin-right: 160px; /* Espacement entre les liens */
        transition: color 1s; /* Ajoute une transition de couleur fluide */
        }

        /* Style pour les liens de la barre de navigation au survol */
        .navbar-nav .nav-link:hover {
        color: white; /* Couleur du texte au survol */
        }

        .navbar-nav .nav-item .nav-link.active {
            font-size: 16px;
        color: black; /* Couleur du texte des liens */
        margin-right: 160px; /* Espacement entre les liens */
        transition: color 1s; /* Ajoute une transition de couleur fluide */
        
        }
        .navbar-nav .nav-item .nav-link.active:hover {
           color: white; /* Couleur du texte au survol */
        }

    

   

  
    


        </style>
    </head>
    <body>
    <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">CamEstheticienne</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="/">Accueil
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/presentation">Présentation</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/prestations">Prestations</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/avis">Avis</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/contact">Contact</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/contact">Connexion</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
<div class="logo"><img src="../assets/bannlogo.png"></div>
<div class="femmbann"><img src="../assets/femmebann.png" width="10px"></div>

        {% block body %}{% endblock %}
    </body>
</html>
